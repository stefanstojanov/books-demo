export default class Book {
    constructor(name, isbnNumber, authorId, unitsSold) {
        this.name = name;
        this.isbnNumber = isbnNumber;
        this.authorId = authorId;
        this.unitsSold = unitsSold;
    }
}